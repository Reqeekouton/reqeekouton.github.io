# 站点基本信息
title: 任亚墨的书房
description: 你似乎来过这里……
baseurl: "" 
url: "https://reqeekouton.github.io"

# 作者信息
author:
  name: "任亚墨"
  email: "public_rymingenu@hotmail.com"

# 社交媒体链接
social:
  twitter: "Rymingenu"
  github: "Reqeekouton"

# 站点设置
markdown: kramdown
highlighter: rouge

# Permalink 设置
permalink: /:categories/:title.html

# 文件处理
exclude: 
  - README.md

# 插件配置
plugins:
  - jemoji
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap

# 自定义变量
custom:
  github_repo: "https://github.com/Reqeekouton/reqeekouton.github.io" # GitHub 仓库链接
  version: "0.0.1-alpha" # 站点或项目的版本号

# 表情符号支持
jemoji:
  # 在此添加更多配置选项，例如设置自定义表情符号
  # 例如，可以配置自定义表情符号映射

# 其他配置
defaults:
  - scope:
      path: "" # 默认配置适用于所有页面
    values:
      layout: default # 默认布局设置为 default
